package LineCount;

import LineCount.GUI.Windows.ProjectManager;

public class Main {

    public static void main(String[] args) {
        // Create a new project manager window(JFrame) from where all the programs logic runs.
        new ProjectManager();
    }
}
